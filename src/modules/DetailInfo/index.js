import * as constants from 'modules/DetailInfo/constants'
import * as actions from 'modules/DetailInfo/actions'
import reducers from 'modules/DetailInfo/reducers'
import * as selectors from 'modules/DetailInfo/selectors'
import sagas from 'modules/DetailInfo/sagas'

const moduleName = 'DetailInfo'

export {
  constants,
  reducers,
  moduleName,
  actions,
  sagas,
  selectors,
}
